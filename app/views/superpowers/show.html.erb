<h2><%=@superpower.name%></h2>

<div><%=@superpower.description%></div>




<%if !current_user.nil? && current_user.id == @superpower.user_id%>
  <%= link_to 'Edit', edit_superpower_path(@superpower) %>
  <%= link_to 'Delete', @superpower, method: :delete, data: { confirm: 'Are you sure?' } %>
<%end%>

<%= link_to 'View all superpowers', superpowers_path %>

<h1>Reviews</h1>

<h3><%= link_to 'Add Review', new_superpower_review_path(@superpower) %></h3>
<% @reviews.each do |review| %>
  <div>
    Rating: <%= review.rating %>
    <%= review.body %>

    Votes: <%= review.score %>
    <%= link_to 'Up Vote', review_votes_path(review), method: :post %>
    <%= link_to 'Down Vote', review_vote_path(review), method: :delete %>


    <%if !current_user.nil? && current_user.id == review.user_id%>
      <%= link_to 'Edit Review', edit_review_path(review) %>
      <%= link_to 'Delete Review', review_path(review), method: :delete, data: { confirm: 'Are you sure?' } %>
    <%end%>
  </div>
<% end %>
