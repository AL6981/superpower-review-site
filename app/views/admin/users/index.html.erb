<h1>Registered Users</h1>

  <% @users.each do |user| %>
    <% if user.role == "member" %>
      <li>
        <%= user.email %>

        <%= link_to 'Delete', @user, method: :delete, data: { confirm: 'Are you sure?' } %>
      </li>
    <%end %>
  <% end%>
